{
  "name": "bloomsage-ecommerce",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Bloomsage — a full-stack e-commerce store for a plant-based skincare brand.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "build": "npm run build --prefix client && npm run build --prefix admin && npm run copy-dist",
    "copy-dist": "node scripts/copy-dist.js",
    "seed": "npm run seed --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin",
    "start": "node server/src/server.js",
    "vercel-build": "npm run install:all && npm run build"
  }
}
