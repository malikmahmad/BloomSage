# Copy this file to .env and adjust as needed.
# The app runs fine with defaults for local development — these mainly
# matter for production deployments.

PORT=4000

# Used to sign JWT auth tokens. Change this to a long random string
# before deploying anywhere real users will sign up.
JWT_SECRET=change-this-to-a-long-random-string-before-deploying
